export type CreateShedulerTYpe = {
  name: string;
  groupNumber: string | null;
  subjectName: string;
};
export type ShedulerType = {
  id: string;
  name: string;
  groupNumber: string | null;
  subjectName: string;
};